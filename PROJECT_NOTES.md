# Project Notes (newest at top)

- 2026-07-06 — **GATE 1 (LIQUIDITY) RUN on the PRO ladder → DOES NOT PASS; in-running direction not advanced.** Governed by the locked `analysis/preregistration_inrunning.md` §4/§6/§7. Built the whole rig first (all green before any verdict): `backtest/pro_stream.py` (stream bz2-in-tar, NEVER extract; hand-rolled `atb/atl/trd` delta reconstruction — best-3 ladders + traded vol + inplay/status at 1s — **cross-checked EXACT vs betfairlightweight**; GB-flat-WIN self-classifier; Betfair tick ladder + matchable-£ fill primitives), `backtest/pro_form_join.py` (run_style_proxy = dominant `classify_runstyle` over a horse's STRICTLY-PRIOR scraped-form comments; reuses the BSP-join normalisers), `backtest/gate1_liquidity.py` (`measure_market`: led runner's FIRST in-running trade ≤2.0 → fill at t+betDelay, matchable-£ within 1 tick both sides → PASS/FAIL vs Y=50%/N=2,000; parallel tar driver). 29 new tests, **suite 95/95**. **Two 2015-16 data-desert problems surfaced + resolved** (race-metadata only starts 2018-02-11): (a) flat/jumps — self-classify from Betfair market name (Hrd/Ch/NHF→jumps), **99.67% accurate vs scraped `type`** (confusion: 4254 Flat right, 14 jumps mislabelled, 396 name-unmatched); (b) run_style — **scraped GB form via rpscrape** for the window AND a **2014-05→2015-04 LEAD-IN** to seed priors (fixed the May-2015=0% / Jun=54% cold start → 81.9% coverage). **RUN (12 tars, combined 2014-16 form, 12 workers, 29 min):** 5,760 GB-flat WIN markets, 57,415 ACTIVE runners, run_style-covered 81.9%, led 12.5%; non-GB WIN markets dominate the pull (US/ZA/FR/IE/SG — GB≈35%, corrects the ingest's front-of-archive "GB 75-100%"). **RESULT: n=1,618 qualifying opportunities (led & first-trades-≤2.0 in running) — BELOW the N=2,000 floor** → formally `INSUFFICIENT_N` (a data-scarcity shortfall, **reported not retuned**, per §4 "widen nothing/lower nothing"). **And even on those 1,618, £100 is matchable in only 27.3% (back) / 18.7% (lay) at t+~1s — far below the Y=50% bar — with MEDIAN matchable ≈ £0** (the book is typically empty / momentarily suspended at the ≤2.0 touch; only a minority carry size, max ~£1.6k back). Example opp: led runner touches 2.0, +1 s → £39.86 available to back, £0 to lay. **VERDICT: liquidity feasibility NOT demonstrated and the evidence points clearly against it** — this RESOLVES the 2026-07-01 free-screen INCONCLUSIVE (its £48k median in-play `ip_vol` was the total-period UPPER BOUND; the point-in-time size at ≤2.0 is ~nil). Per §7, liquidity gate not passed → **Q2 edge gate NOT run** (gated behind liquidity). Honest caveat: n<N means it is formally underpowered for a clean PASS/FAIL AT the pre-registered N; but the fill evidence (median £0; 19-27% at n=1,618, 95% CI nowhere near 50%) makes the practical FAIL robust — buying more PRO data to reach N=2,000 would very likely only confirm it. No parsing/analysis beyond Gate 1. Artifacts: `models/gate1_liquidity_results.json`; commits pre-reg `522f5e2`, code `1242434`, results (this).

- 2026-07-04 — **Betfair PRO tars INGESTED (verify → move → validate; NO parsing).** Completes the download the 2026-07-02 "ABORTED (source absent)" note was waiting on, and delivers the PRO full-ladder + traded-volume stream the 2026-07-01 in-running liquidity gate flagged as PAID/pending (necessary to turn that INCONCLUSIVE liquidity screen into a definitive pass/fail). **Inventory:** 11 archives on `H:/` (`data.tar`, `data (1..10).tar`), 44,302,310,400 B = **41.26 GiB**, **138,914 members** total; all 11 `tar -tf` clean (exit 0, zero real errors — only benign `LIBARCHIVE.creationtime` PAX warnings ⇒ archives written by bsdtar/libarchive, not corruption). One `Unconfirmed 227369.crdownload` (660 MB, stalled at 21:20) **SKIPPED** per spec — partial in-flight download, left on H:. **Layout:** `PRO/YYYY/Mon/D/<eventId>/<marketId>.bz2` (PRO-tier prefix; each event dir carries an `<eventId>.bz2` meta file + per-market `1.*.bz2` tick streams). **Spans/gaps** (derived from member PATHS only — no content parsed): overall **2015-06-01 → 2018-05-14**, **341 distinct racedays, 10,676 events, 128,252 markets**. Real shape = a **continuous block 2015-06-01 → 2016-04-30 (334 racedays, complete bar 2015-12-25 = Christmas, no GB racing → not a real gap)**, after which coverage falls off a cliff: from 2016-05 onward only **7 isolated stray racedays** exist (2016-05-01, 2016-06-04, 2016-08-06, 2017-03-17, 2017-06-24, 2017-08-05, 2018-05-14) — every other month 2016-05 → 2018-05 is entirely or near-entirely absent. The headline "68% missing / 281-day largest gap" is that post-Apr-2016 desert. **Coverage — SET NOW COMPLETE:** intended set per user = **May-2015 → Apr-2016**. Original 11 tars covered Jun-2015 → Apr-2016 (all of May-2015 was absent). **TOP-UP 2026-07-04 23:05:** a 12th archive (Chrome `data.tar`, 4,773,432,320 B / 14,699 members, `tar -tf` clean) delivered **May-2015 complete (all 31 days)** + 3 stray dup days (2×Jun-2015, 1×Apr-2016, already held) — moved to `data/historical/betfair_pro/data_may2015.tar` (renamed to avoid clobbering the existing `data.tar`; byte+member verified, H: source deleted, crdownload untouched), PRO fields spot-checked on `1.114449698` (evtType 7, GB, atb/atl/trd, sub-second pt). **Dataset is now one continuous block 2015-05-01 → 2016-04-30 = 365 racedays** (only 2015-12-25 Christmas absent = no GB racing → not a gap). Totals: **12 tars, 49,075,742,720 B = 45.71 GiB, 153,613 members.** The 7 stray 2016–2018 days remain OUTSIDE the intended window (extra/noise, ignore). Stalled `Unconfirmed 227369.crdownload` (660 MB) still parked on H:, still skipped. Corrects an earlier draft of this note that called the core "→ 2016-08-06 (~337 days)" — the Aug-2016 date is a single stray day, not the end of continuous coverage. Tars are NOT date-ordered — dates scatter across archives. **Move:** copied `/mnt/h` → `data/historical/betfair_pro/` on WSL ext4 (`/dev/sdd`, 939 G free; ~185 MB/s), verified byte-size AND member-count per file (all 11 MATCH), then deleted the 11 H: sources (true `mv`; crdownload untouched). Dest = 41.26 GiB, git-ignored via existing `data/historical/`. **Validation (3 sample markets — first/middle/last tar; single-file `tar -xO | bunzip2`, no full extract):** PRO-tier CONFIRMED — `atb`/`atl`/`trd` ladders all present on active markets in data.tar (`1.119038316`), data (5) (`1.120521207`), data (10) (`1.124052351`; the first pick `1.124170271` was a dead IE market with 4 msgs & no ladders, so re-sampled); sub-second `publishTime` deltas present (min 0–44 ms → genuine tick stream). Over 60 front-of-archive markets: **eventTypeId==7 = 100%** (horse-racing-ONLY pull, no other sports) and **GB ≈ 75–100%** across tars (predominantly GB, some IE — NOT GB-only). **GATE/BLOCKER — this is INGEST ONLY, no parsing / no analysis performed.** Pre-registration (Q1–Q4 + era weighting + discovery/holdout split + faller cost + per-course stratification) MUST be committed before any gate work touches this data.

- 2026-07-03 — **Test 4: LAY-SIDE selection (Free Experiment Catalogue).** Question: Stage-1's "value" picks drift OUT +31.66% vs +14.12% baseline — wrong side for backing, right side for laying; does laying them yield positive lay-CLV OOS after the hardened verdict? Built the lay harness first, maths pinned before scoring: `backtest/lay_clv.py` (lay-CLV = `bsp/struck − 1`, positive = price drifted after our lay = we laid shorter/better than close; fixed-LIABILITY P&L `stake=L/(o−1)`, win `+stake(1−c)`, lose `−L`; plus fixed-STAKE P&L; net **5%**) + `backtest/test_lay_clv.py` (15 tests incl. lay=−back duality; all 66 in `run_tests.py` green). Scorer `models/score_lay_selection.py` flips the EXISTING selections to lay — no new features, no re-tune — reading `stage2_scored.csv` (`model_prob` vs pre-off `market_prob`; struck=wap; leakage-safe, never BSP). Split discovery(≤2023-12-31)=val | holdout. Referee = (1) price-band-stratified lay-ALL null (each pick vs mean lay-CLV of ALL runners in its own BSP band, must beat on val AND holdout) + (2) Brier gate (blend beats BSP on selection's holdout horses). **V1 (lay the back-value picks, model_prob>market_prob; n=129,018 hold): ARTIFACT-NAMED.** Raw holdout lay-CLV **+23.48%** looks huge but the band-matched lay-ALL null is **+24.90%** → edge **−1.43%** (val −1.14%); 79.6% of the book sits in bsp>12 (lay-CLV +27.6% there vs +7.4% in bsp≤12) and every band's edge-vs-lay-ALL is ≤0 — the +32% drift was BSP-BAND COMPOSITION, not the model. Exactly the named artifact: longshots drift WAP→BSP and the selection just inherits the FLB/timing. Fixed-liab ROI@struck +0.51% but @BSP −0.26% (pure wap-vs-close timing), Brier NO (blend 0.04758 ≥ BSP 0.04746). **V2 (control, lay over-priced picks, model_prob<market_prob; n=84,430 hold): PRICED.** Passes gate 1 (edge over band-null +2.18% hold / +1.74% val — beats the null on BOTH) but FAILS gate 2 (blend Brier 0.15059 ≥ BSP 0.15002 — no probability edge) → CLV-relative only. Instructive: V2 shows the two gates do independent work — a positive, stable band-null edge with no forecasting skill behind it is still priced. Net: laying the drift cohort does NOT beat the band-matched lay-ALL baseline; the drift is the band composition talking. Outputs `models/lay_selection_results.json`.
- 2026-07-02 — **Betfair PRO tar move — ABORTED (source absent). No move performed.** Task: move `/mnt/c/Users/gabriel/Downloads/data(1).tar` (stated 45.8GB Betfair PRO full-ladder archive — the paid historicdata.betfair.com pull the 2026-07-01 note said was needed) → new `data/betfair_pro/raw/pro_gb_racing_2015_05__2016_04.tar`, via `mv` (C: is out of space). **Verified read-only before touching anything, per the task's own "verify exact path first" discipline — and aborted:** (1) **Source does not exist.** `/mnt/c/Users/gabriel/Downloads/data(1).tar` → No such file. A `find` for any >1GB file under Downloads/OneDrive/Desktop and any `*betfair*`/`*pro_gb*`/`*data*.tar >1GB` across C: and H: found NOTHING near 45.8GB — largest tar anywhere is an unrelated 14GB `/mnt/h/WSL/backup/ubuntu-backup.tar`; Downloads' big files are movies/ISOs. No `.crdownload`/`.part` partial either. The download appears not to have completed (or landed elsewhere). (2) **Free-space check PASSED (not the blocker):** `df` shows H: 900G free, WSL root `/dev/sdd` 939G free, both ≫100G; C: is indeed 95% full (48G free), consistent with the "out of space" claim. (3) **Destination premise off:** the task says "project root on H:", but the project actually lives on the WSL native disk `/home/gabriel/projects/racing_project` (`/dev/sdd`), not `/mnt/h` (which is a Windows/Xbox gaming drive: Program Files, WindowsApps, .GamingRoot). Moot given (1). **Nothing moved, no dir created, no file overwritten.** Downstream format-validation not run (no file to validate; also the "format-validation steps from the previous prompt" were not present in this session's context). **USER ACTION NEEDED:** re-download / locate the PRO tar, confirm its exact path + intended destination drive (WSL `/dev/sdd` has more free space than H: and is where the project actually is), then re-run the move.
- 2026-07-01 — **IN-RUNNING Stage 1, LIQUIDITY GATE first-pass (FREE screen, no purchase)** + commission correction. USER CONFIRMED N=2,000/X=£100/Y=50%, signal=front-runner(run_style_proxy=led)@in-running price≤2.0. COMMISSION FIX: my Stage-0 doc carried 5% over from clv.py `DEFAULT_COMMISSION` by default; the canonical brief `prompts/InRunning_Stage0_PreRegistration.md` (pre-existing, I'd missed it) specifies **2%** — corrected in `Strategy_Direction_InRunning.md` (commission is moot for the liquidity gate anyway; only bites at the edge gate). KEY: the joined dataset ALREADY carries Betfair in-play aggregates `ip_min`/`ip_max`/`ip_vol` (99.8% populated), so a FREE upper-bound liquidity screen runs without buying anything — `models/inrunning_liquidity_screen.py`. RESULT: 21,416 qualifying opps (led & ip_min≤2.0; ≫ N_MIN 2,000); TOTAL in-play matched volume on these runners is LARGE (median £48,723, p10 £17,679); **100.0% have ip_vol≥£100** (disc & holdout both 100%). So the cheap kill did NOT fire. BUT ip_vol is TOTAL in-play across the whole period/all prices/both sides = a NECESSARY-NOT-SUFFICIENT UPPER BOUND on point-in-time size at ≤2.0 at t+1s; the screen can only decisively FAIL, not PASS. VERDICT: **NOT KILLED / INCONCLUSIVE at zero cost** — a definitive pass/fail needs PRO-tier full-ladder+volume data (historicdata.betfair.com, PAID). CONSTRAINT surfaced: I cannot autonomously purchase Betfair historical data (needs user's account/payment/manual download), so "buy data" is a user action. STOPPED at liquidity per instruction; NOT proceeding to the edge gate; awaiting user decision on whether to buy the ladder.

- 2026-07-01 — **IN-RUNNING direction, STAGE 0 pre-registration drafted** (`Strategy_Direction_InRunning.md`) — AWAITING USER CONFIRMATION of N/X/Y before Stage 1. New/separate direction (Betfair in-running trading viability; the long-flagged "next shot = non-public/in-running data"). Stage 0 = document only, NO data bought, NO backtest code, NO signal fit. Pre-registers a LIQUIDITY-FIRST gate as the primary/cheapest kill (prior belief: likely fails here): proposed N=2,000 qualifying opportunities, X=£100 target matched stake, Y=50% (must match ≥£X in >50% of opps under a conservative fill model) — liquidity measured over the full qualifying set (market property, not fitted, no split). Conservative FILL MODEL pinned now (the crux/artifact source): needs Betfair historical in-running price + TRADED-VOLUME-at-price stream (not BSP/LTP); latency L=1.0s, cross the spread, match=min(£X, avail within 1-tick slippage) at t+L, 5% comm; rejects the "displayed price, unlimited size" fantasy. SECONDARY edge gate (only if liquidity passes): matched-subset net-comm ROI beats a PRICE-BAND-STRATIFIED structural in-running null by ≥+1% (EDGE_TOL) AND >0 absolute, discovery/holdout-corroborated; hindsight-perfect-fill CANARY must show a large edge else INCONCLUSIVE (in-running analog of the rpr canary). Kill criteria incl. "edge only under hindsight fill, gone under realistic fill = latency/liquidity artifact" (in-running analog of the WAP-vs-close timing artifact). Qualifying-opportunity signal is a PLACEHOLDER to confirm (default: run_style_proxy=led front-runner, enter when in-running price first ≤2.0). Stage 1 (buy data → liquidity gate → edge test) authorized only after user confirms the numbers.

- 2026-07-01 — **BUGFIX (live-card horse history silent zero-match)** in `features/history_join.py` (+5 regression tests in `backtest/test_history_join.py`). SEVERITY: blocked trusting the runner view / scanner Tier-2 on ANY live card — every horse showed career_runs=0 / all horse-level Layer-2 null (career win%/place%, or_trajectory, class_change, dslr, C/D/CD flags, run_style_proxy), silently, for horses with plenty of history. ROOT CAUSE (found via the Worcester 12:20→13:20 demo): the historical CSV keys horses WITH the Racing-Post country-of-breeding suffix ('Atreides (IRE)', 'Bettys Tiara (GB)') but LIVE racecards give the clean name ('Atreides'), so `HistoryIndex.horse` lookups missed 100% on cards. Trainer/jockey SRs populated fine (those names carry no suffix), masking it. Same class as the earlier course-name byte-match issue — a silent zero-match is worse than a loud error. PROOF: querying the 726k index with 'Atreides'→0 runs but 'Atreides (IRE)'→24. FIX: `canon_horse(name)`→(base, country) strips a trailing `(CC)` code + normalises case/whitespace; `prior_horse_runs` now tries the EXACT name FIRST (so the HISTORICAL bridge/backtest/materialiser path — suffixed on both sides — hits the exact branch and is byte-for-byte UNAFFECTED) and only falls back to the canonical base on a MISS. Ambiguity guard: a base mapping to >1 distinct country code ('X (IRE)' vs 'X (USA)' = possibly different horses) is REFUSED, never silently merged; case-only variants of the SAME country ('Gwash (IRE)'/'gwash (IRE)') merge. Built once at index init (self._canon_horse / self._canon_ambiguous), reuses existing per-key run/ord lists (no copy for single-variant horses). REAL-DATA IMPACT: 69,741 horses → 69,488 canon bases, only 124 (0.18%) ambiguous/refused; all 11 Worcester runners now populate (career_runs 9–31, win%/place%/or_traj/class_change/dslr/run_style all live). Historical path confirmed unaffected (exact-match short-circuit + `test_exact_suffixed_lookup_unaffected`; delete-future invariance still passes). SUITE: 46→51 tests, all pass. (NB run_tests.py discovers only backtest/*; the models/ probe verdict tests are run manually.)

- 2026-07-01 — **NARROW-SUBPOPULATION probe: four mechanism-first slices** (`models/score_slices.py` + `models/test_slices_verdict.py`, 4 tests). Parallel analysis; tests whether the rolling-form feature set finds edge inside four narrow slices each picked for a market-softness MECHANISM, with the null computed WITHIN the slice (never whole-field). ALL FOUR **PRICED**. (NB: the brief titled it "four mechanism-first slices" but the specific four were truncated in transmission — I chose four defensible race-level stand-ins; the harness `SLICES` dict is generic so swapping predicates is one line. Flag if the intended four resurface.) SLICES (holdout races): lowclass=Class 6-7 flat (soft money; 4,718), bigfield=>=16-runner flat handicap (pricing complexity; 280), staying=>=12f flat (stamina/form-over-speed; 2,078), sellers=sellers/claimers flat (softest money; 76). METHOD per slice: per-slice conditional-logit WIN model on rolling features (discovery-fit), Benter blend vs market, evaluated on holdout with (a) BRIER gate = blend beats market within-slice by >1e-4, (b) MONEY gate = blend top-pick ROI@BSP beats the WITHIN-SLICE price-band-stratified back-all@BSP null by >+1%, (c) CANARY = rpr-augmented model must beat market on the slice's holdout Brier (validates the slice is powered enough to SEE an edge — pre-registered, printed up front), (d) corroboration on discovery. RESULTS: every slice's blend collapses onto the market (blend market-share 92-98%; blend-minus-market holdout Brier ≈ 0.00000, range -0.00004..-0.00001 — NO probability edge anywhere), while the rpr canary crushes the market on Brier everywhere (+0.032..+0.067) → the rig is POWERED and simply finds no rolling-feature edge. THE TRAP (hardening earned its keep): bigfield holdout @BSP edge looked spectacular **+30.38%** and sellers **-16.98%**, BUT these are small-sample betting NOISE (280 and 76 one-per-race bets, ROI SE **±14.5%/±15.2%**, bigfield unstable disc +5.48%→hold +30.38%) — DENIED rule-in solely because the AND-gate requires a Brier edge too (there is none); added a `MIN_HOLDOUT_BETS`=500 flag so a thin betting channel is explicitly labelled noise in the verdict rather than read as edge. Two well-powered slices (lowclass 43,764 runners, staying 17,338) show clean no-edge with tight numbers (@BSP edge +0.48%±2.25%, +3.34%±3.23% = ~1 SE). VERDICT ladder: canary-unpowered→INCONCLUSIVE (not priced — an underpowered null looks identical to a clean one); powered+no-edge→PRICED; powered+Brier+money+corrob→ruled-in (none reached it). CROSS-CUTTING: narrowing to mechanism-first soft slices does NOT surface edge — the market is efficient within Class 6-7, big fields, staying races AND sellers alike; combined with tonight's target-reframing and timing/behaviour probes, public/free edge is exhausted across POPULATIONS too, not just features. Next remains non-public/orthogonal data.

- 2026-07-01 — **CLV / PRICE-MOVEMENT probe** (`models/score_price_drift.py` + `models/test_price_drift_verdict.py`, 5 tests) — a DIFFERENT kind of question (market TIMING/BEHAVIOUR, not horse KNOWLEDGE): can pre-race features predict which horses' prices move DIFFERENTIALLY morning→close, beyond the common market-wide drift? Parallel analysis, same discipline. RULED OUT (**PRICED**) — but with the most interesting texture of the night. DATA: `morning_wap` 98.9% populated; drift = bsp/morning_wap−1 has disc median **+11.4%** (prices DRIFT OUT morning→close on average; mean +39% dragged by longshots, p90 +124%) → the money-CLV of striking at morning is NEGATIVE on average (median −10.7%), i.e. structurally the CLOSE is the better BACK price. THE NULL (same FLB trap, one level up): the common drift is price-dependent (favourites steam, longshots blow out), so the honest baseline is the MORNING-PRICE-BAND-STRATIFIED common drift — each pick benchmarked to the avg move of all runners in its own morning band; a naive "predict steamers" = "pick favourites" for free otherwise. TWO GATES on holdout (split @2023-12-31; 720,022 runners, holdout 213,103): (1) PREDICTION — residual-drift ridge (drift minus its band mean) on the pre-race rolling set: holdout resid **R²=+0.0119** (disc +0.0115) — SMALL BUT REAL & STABLE: features genuinely predict differential drift (more than any of the 7 prior knowledge tests, which found nothing). (2) MONEY kill-test — back the model's predicted steamers at morning, settle BSP, net comm; the decisive metric is the ABSOLUTE roi_morning surviving commission, NOT the differential. RESULT: in-band (price-balanced) predicted-steamers holdout **ROI@morning −3.03% ±3.31%, mean CLV −1.96%** (they DON'T even beat the close), edge_vs_BSP −3.11%, DIFF +2.92%; whole-pop steamers −3.18% (longshot-concentrated, mornAvg 15). The +2.9% "differential" is positive ONLY because the picks lose LESS than their band's common drift (back-all morning-strike edge −7.58% holdout) — a CLV-relative artifact, not money. POWER CANARY: wap-move (wap/morning−1, a later pre-off price = partial peek at close) resid **R²=+0.712**, corr 0.90 — the rig CAN see a differential-mover signal when one exists, so the null is informative. ANCHOR (leakage): |corr(pre-race feature, drift)| all weak (≤0.16, mostly <0.10) vs canary 0.90 — the drift label leaks no same-race info into the features. VERDICT: **PRICED** — a real, OOS-stable differential-drift SIGNAL exists but is NOT harvestable: predicted steamers get negative CLV and lose ~3% at morning because prices structurally drift OUT (close is the better back price) and the signal lives in the FLB longshot tail; laying the drifter tail is untradeable extreme-longshot (mornAvg 88). Bug fixed mid-build: first verdict wrongly RULED-IN on the positive differential alone — corrected to require absolute roi_morning to survive commission (the whole point: "edge that survives commission"). CROSS-CUTTING: the timing/behaviour axis is the FIRST to show a genuine pre-race-predictable signal, but it too is priced/un-harvestable — public-information edge (knowledge AND timing) now looks comprehensively exhausted; next shots remain non-public/orthogonal data (timestamped strike for a TRUE CLV, in-running, sectionals). `Strategy_Direction_CLV_Price_Movement.md` referenced in the brief is NOT in the repo (spec was inline).

- 2026-07-01 — **Target C EXTENSION: is the 2nd-favourite's ranking CONDITIONALLY reliable?** Built + tested SOLO (`models/score_target_c_conditions.py` + `models/test_target_c_conditions_verdict.py`, 4 tests), reusing Target C's exact setup (same sub-race population, same split @2023-12-31, same 2nd-fav null). RULED OUT (**PRICED**), as anticipated. LABEL: per sub-race, Y=1 iff the 2nd-favourite (min-BSP non-fav) IS the best-of-rest (its flat holdout rate = Target C's 32.60%). QUESTION: do pre-race conditions (draw_norm, going, field_size, n_nonfav, dist_f, class, course handedness/shape) predict WHEN the 2nd-fav is more/less reliable than that flat 32.60%? THE HARDENED GATE (the honest bar): NOT "does a condition move the raw correctness rate" (most of that is already in the PRICE that made the runner 2nd-fav) but "does knowing the condition improve CALIBRATION BEYOND THE MARKET'S OWN PROBABILITY for that runner" — i.e. logistic **M2 (market_logit + conditions)** vs **B1c (market_logit alone, recalibrated)** on holdout Brier. RESULT: M2 0.20364 vs B1c 0.20340 on holdout = **−0.00024 (WORSE OOS)**; conditions overfit on discovery (+0.00016) and add nothing beyond the price. POWER CONFIRMED by an anchor/canary: market+**rpr** (POST-RACE) beats B1c by **+0.01349** (>1e-4) — the harness CAN see a real conditioning signal when one exists, so the null is informative, not a dead test. Conditions-only (no market) Brier 0.21414 barely beats uniform 0.21974 and is far worse than market 0.20340. SUBSET PROBE (the illuminating part): **n_nonfav** is a huge, STABLE (disc↔holdout) driver of RAW 2nd-fav correctness — 2 non-favs→68%, 3→51%, 5→39%, 9+→25.6%, monotonic (14→−15 SD from base); class (C4 +6.4sd / C6 −7.8sd) and draw_NA (+5.6sd) also deviate — but ALL of it is already priced into the 2nd-fav's BSP, so none beats the market's own probability (M2≤B1c). VERDICT: **PRICED** — the conditioning that predicts when the 2nd-fav is right is already in the price that made it the 2nd-fav. BUG CAUGHT + fixed mid-build: `dist_f` carries an 'f' suffix (documented gotcha — [[layer2-history-join]]) so plain float() returned None for EVERY row → NaN-poisoned the conditions models silently until a tolerant leading-numeric-token parser (`dist_num`) + a zero-valid-values guard were added (the guard now hard-fails instead of going NaN). CROSS-CUTTING: closes the Target C thread. Reframing (who-wins → best-of-rest → WHEN-is-best-of-rest-market-right) never opened an edge — the market prices its sub-population ranking, and the reliability OF that ranking, just as efficiently. Per plan, this was the one honest conditioning shot; NEXT direction is CLV / price-movement (non-public / timestamped-strike), regardless of this outcome.

- 2026-07-01 — ALTERNATIVE-TARGET round, **Target C = best-of-rest (favourite excluded)** built + tested SOLO as a PARALLEL analysis, then RULED OUT (PRICED). NOT a change to the main pipeline — new file `models/score_target_c.py` (+ `models/test_target_c_verdict.py`, 5 tests), win-target verdict logic in `run_strategy.py` untouched. SAME materialised leakage-clean features (rolling form: stage-1 base or/draw/lbs/age + build_rolling f_* — ran `features/build_rolling.py` to materialise `joined_gb_2018_2026_feat.csv`, gitignored), DIFFERENT label. POPULATION: each race filtered to NON-FAVOURITES (drop the single lowest-BSP priced runner). LABEL: "best-of-rest" = lowest finishing pos among the non-favs (dead-heat→min BSP); exactly one positive per sub-race. 82,231 races → 81,765 usable sub-races (dropped 455 <3-priced, 11 no-label); split @2023-12-31 = discovery 57,212 | holdout 24,553 sub-races (well above THIN=2,000). THE CORRECT NULL (the whole point — NOT a naive 1/(field−1), which would hand a fat edge for free off the market's own ranking, the same shape as the FLB hole): the market's ranking of the sub-population = the 2nd-FAVOURITE (lowest BSP among non-favs), expressed as (a) argmax hit-rate and (b) q_i = softmax(log 1/bsp) renormalised WITHIN the non-fav group; PLUS a price-band-stratified @BSP null (same 14 bands as the main fix) since non-favs span a wide price range. RESULT — priced on every leg: (1) BEST-OF-REST IDENTIFICATION: 2nd-fav hits 32.60% (holdout) > blend 32.36% > model-alone 23.40% — the market's ranking is the BEST identifier; our public model can't beat it (blend−2nd-fav = **−0.24%** holdout, −0.26% disc). (2) BRIER GATE (the decider): blend−market margin = **+0.00009** holdout (≤1e-4 ⇒ ~market), blend collapses to **99.2% market weight** (Benter fit: log-model +0.007 vs log-market +0.908); model-alone Brier 0.10736 is WORSE than market 0.10014 — no probability edge. (3) THE TRAP: backing the model's top non-fav @BSP looked fat (+7.25% disc / +3.14% holdout ROI, +11.85%/+7.98% stratified-edge) — but it's the FLB/longshot artifact the brief predicted: picks are longshots (mean BSP 12.73), holdout ROI **+3.14% ±5.11%** (0.6 SE from zero) and it HALVED out of sample; the honest tight-SE picks (2nd-fav/blend, mean BSP ~5.3) sit at ROI@BSP ≈ −4.7% ±1.3% ≈ −commission with ~0 stratified-edge. Exactly "a CLV-style edge with no probability improvement does not rule in." ANCHOR TEST on the LABEL (within-sub-race Spearman vs finish, non-fav group): or −0.058 (pre-race anchor), rpr −0.850 (post-race leak — test STILL bites on this population/label), all rolling features in the pre-race band (worst f_trn_sr −0.129) → best-of-rest is a normal pre-race-safe target, the re-labelling introduces no leakage. VERDICT: **PRICED** (holdout-decided). CROSS-CUTTING: reframing the question (best-of-rest vs who-wins) did NOT open an edge — the market prices its own sub-population ranking just as efficiently; consistent with the standing conclusion that public/free-information edge is exhausted. Targets A (beat-the-favourite) and B (place) NOT built this round per instruction. `Strategy_Direction_Alternative_Targets.md` referenced in the brief is NOT in the repo (task was fully specified by the prompt itself); flag if it appears.

- 2026-06-30 — Verdict baseline HARDENED against the favourite-longshot hole (verdict-baseline only; anchor/leakage logic untouched). Commit eec32a1 (on top of the clean weather-probe commit 482565a; not pushed — git identity still personal email). THE BUG: `racing_rulebuilder/run_strategy.py`'s composition-fair null de-overrounds each race by ONE per-race factor (p_i=(1/bsp_i)/Z), which strips only the overround LEVEL, not favourite-longshot price COMPOSITION — and because short prices return more @BSP than their de-overrounded implied prob, any favourite-skewed no-skill rule beats it for free. CONFIRMED on `trainer_course_sr >= 0.20` (split @ 2023-12-31, holdout n=26,876): the fair-null baseline (-4.11%) was MORE negative than the plain full-field baseline (-2.86%), inflating the holdout headline to +1.96% → false RULED-IN, even though the selection's own ROI@BSP is -2.15% (it LOSES money). FIX 1 — PRICE-BAND-STRATIFIED NULL (new headline `edge_bsp`=`edge_bsp_strat`): in the same single streaming pass, bucket the full field's back-all@BSP P&L into 14 BSP odds bands (`_PRICE_BAND_EDGES`, fine at the favourite end; thin band <`MIN_BAND_N`=50 falls back to the global ROI) and benchmark each selected horse against the full field's actual @BSP return IN ITS OWN BAND — so a rule that merely skews toward favourites is measured against the real return of backing those favourites. fullfield + fair nulls KEPT for display. FIX 2 — BRIER-CORROBORATION GATE: to read RULED-IN the edge must clear the floor, hold its discovery direction, AND the blend must beat BSP on Brier over the selection's OWN holdout horses (read-only from `models/stage2_scored.csv` via memoised `_load_blend_map`; both probs per-race-normalised so Brier is comparable; <`MIN_BRIER_COVERAGE`=0.5 coverage → un-corroborated → priced). A CLV edge with no probability edge behind it = the FLB artifact → priced. RESULT: trainer_course_sr now reads PRICED. The stratified null trims the headline +1.96%→+1.33%, but that ~+1.4% residual is STABLE across band granularity (8→64 bands) = a genuine CLV-relative skew with NO forecasting skill (course = proxy for general trainer hot-form), so the Brier gate (blend 0.12244 ≥ BSP 0.12180, 100% covered) is what forces priced. BOTH fixes are needed: the null alone would still barely rule in (+1.33% > +1% floor); the gate is the decisive backstop. TEST: `backtest/test_verdict.py` (7 tests, all pass; suite now 35/35) — Test A drives the full pipeline on a synthetic FLB-biased EFFICIENT-market fixture (fav @BSP 2.0 wins exactly 1/2 = raw implied; 4 longshots @6.0 split the rest = 1/8 each < 1/6 implied) where the OLD fair-null edge is large+positive (the hole) but the stratified edge collapses to ~0 and the verdict is PRICED even with a monkeypatched PERFECT blend that WOULD pass the Brier gate (proving the composition fix alone blocks the false rule-in); Test B unit-tests `_verdict` so the gate alone flips ruled-in↔priced. CROSS-CUTTING: this was the LAST open thread; with the verdict baseline now favourite-longshot-neutral AND probability-edge-gated, the project's standing conclusion holds with no false positives — all free families priced, public/free-information edge comprehensively exhausted; next shots are non-public/orthogonal data (timestamped strike, in-running, sectionals, pedigree), i.e. money/data not more free features.

- 2026-06-29 — Weather / visibility probe (FOURTH free family) built + tested SOLO, then RULED OUT (priced + untestable). Scope was hedged to a VISIBILITY-ONLY probe at the user's call after Block 0. BLOCK 0 BLOCKER (the decisive audit): the going-shift-vs-declared-going feature — the ONE weather family with a real (lightly-priced) prior — is NOT BUILDABLE: rpscrape `going` is a single race-day string with NO declaration timestamp / going-stick / "going updated" field (joined data AND raw form source identical 41-col schema; `going.py` is only a static normaliser). `off` (HH:MM) exists (picks the weather hour) but says nothing about when going was declared. No Met/GIS layer exists. So going-shift collapses to raw weather; user chose the visibility-only path (skip going-shift + air-quality). DATA SOURCE GOTCHA: Open-Meteo ERA5 archive API returns `visibility`=ALL-NULL (not in ERA5; temp/etc fine) — had to switch to historical-forecast-api (archived hi-res model runs) which DOES carry visibility (units=m). COVERAGE CLIP: visibility only populated from ~2022-03-01 (null before) → usable window 2022-03→2026-06 = 53% of the 2018-2026 rows; val(2024)+holdout(2025-26) FULLY covered, train loses 2018-2021 (train=covered 2022-23). FETCH: `data/weather/fetch_visibility.py` one big call per course over its covered range (64 calls, NOT 6k course-days — API is volume/latency-bound; full-year 9-var ~78s, trimmed to 4 fog vars visibility/temp/dewpoint/wind), cached per course (resume-safe), 600s timeout + backoff; 376,821 runner rows in window, 100% got visibility. `data/weather/course_coords.py` (Block 1) maps all 65 GB course strings → 60 sites, 0 unmatched (AW/July suffixes share grid cell; built from scratch — the "scaffold" the brief referenced does NOT exist in repo). === THE GATE (Block 4, decisive) === TRUE FOG (<1km — the only band with a racing mechanism: inspections/abandonments/affected running) occurs in just 17 train / 4 val / 7 holdout races over the whole window — an order of magnitude below the min-cell rule (~2,000 selections; 4-7 races≈30-80 runners). The only well-populated band, <5km (val 1034 / holdout 878 races), is mild haze with NO mechanism (a grey day the going line already sees). MECHANISM AND STATISTICAL POWER ARE DISJOINT → the fog flag is untestable as specified. STRUCTURAL: a race-CONSTANT fog flag CANCELS in the conditional logit anyway (confirmed: neg_log_vis within-race Spearman degenerates to a tie-artifact +0.9998 because it's constant within race) — so the only identifiable, powered test is a within-race INTERACTION. BLOCK 5 (`models/score_weather_lay.py`, `features/build_weather.py`; covered-window flat/AW rows w/ draw+visibility = 250,402; train 108,804 / val 59,068 / holdout 82,530): (A) blend-weight test — baseline {or,draw,lbs,age}=5.87% → +vis×draw interaction=5.82% = -0.05% (NO rise; same down-weighting as rolling/handicap; speedfig remains the only family that ever RAISED weight, +2.98%). Interaction betas tiny (vis_x_draw -0.025, fog5k_x_draw +0.001≈0). Blend Brier does NOT beat BSP (val .08582 vs .08559; holdout .08697 vs .08681). LAY kill-test as expected: NULL lay-ALL@BSP -3.06%/-6.72% (=commission); big wap ROI/CLV (val +9.67%/+20% CLV) is the WAP-vs-close timing artifact; blend lay-selected beats lay-ALL@wap by only ~+2.2/+1.4pts and is ~91% market = market filter, not weather signal. (B) market-efficiency SEGMENTATION by visibility band — NO band where blend stably beats BSP: the single val "win" (<2km blend-BSP -0.00057) FLIPS to +0.00064 in holdout (tiny cells, 52/42 races) = out-of-sample noise; fog races if anything are MORE efficiently priced (lower BSP Brier in <2km), market is not less efficient in fog. LEAKAGE: the actual feature vis_x_draw within-race Spearman vs pos = -0.0216 — inside or's clean pre-race band (or -0.10), nowhere near rpr -0.88. Clean. VERDICT: weather/visibility PRICED OUT (no market-orthogonal edge, does not beat BSP, lay edge dies at the @BSP null — identical land-pattern to rolling+handicap) AND true-fog UNTESTABLE (too rare). FOURTH free family ruled out; weather removed from the free hypothesis space. Does NOT justify paid weather data. CROSS-CUTTING: rolling, handicap, speed, weather — all four free families now tested; all priced (speed the strongest model-leg ingredient, none beat BSP). Public/free-information edge comprehensively exhausted on this market → next shots are non-public/orthogonal data (timestamped strike for true CLV, in-running, TPD sectionals, pedigree), i.e. money/data not more free features. `data/weather/{course_coords.py,fetch_visibility.py,analyze_fog.py,build_weather.py←features/}` committed; cache + weather_by_race.csv + joined_*_wx.csv gitignored (regenerable). Block 0 brief docs `Free_Experiment_Catalogue.md`/`Feature_Catalogue` STILL MISSING from repo.

- 2026-06-23 — Speed figures (`features/build_speedfig.py`, own module, sibling of build_rolling/build_handicap, NOT fused) built leakage-safe + tested on the LAY target, then KEPT (reusable, no edge — same land-pattern as handicap). FLAT only. THE FIGURE: `secs` (per-runner finish time, 99.7% of flat, lengths-derived = winner clock + beaten-lengths) expressed as deviation from a PAR time per (course, dist_f, surface) cell. Par = MEDIAN secs (robust). 321 cells, 311 usable (>=30 runs) = 100% of runs — matches the audit's 311. Coarse surface-aware GOING-band correction (per-cell-then-pooled fractional shift, applied to par): turf monotone firm -1.21% / good -0.49% / good_soft +1.24% / soft +4.09% / HEAVY +6.65% (heavy markedly slower than good, +7.14% — sanity passes); AW tighter (std_fast -1.30 / standard -0.14 / std_slow +0.72). speedfig_raw = (par_adj - secs)/par_adj (faster=higher), mean ~0, sd 0.025. HONEST LIMIT (do not overclaim): lengths-derived + self-standardised, NO authoritative par/going/WFA scale — good for ranking, weak as absolute cross-race figure; fine as a proxy. CONSTRUCTION DECISIONS (both made at user's call before any fit): (1) PAR + going shift estimated TRAIN-ONLY (<=2023), frozen to val/holdout — train-only barely moved the figure (heavy-good +7.28%->+7.14%), confirming par is a stable STRUCTURAL constant, not absorbing outcome; (2) Stage-2 features CLIP each prior figure at train-p01 floor (-0.0751) so a tail-off/pulled-up run (min sf_raw -1.41; ~1% below floor) can't poison its NEXT-run feature — descriptive column left UNCLIPPED. ANTI-LEAKAGE for the build: speedfig_raw is POST-RACE/descriptive (monotone with finishing pos WITHIN a race because secs is lengths-derived — expected, harmless, NOT a feature); par is across MANY races so a horse's own outcome never sets its baseline; NOT z-scored within race (confirmed: same Kempton-8f cell, fast-race winner +0.054 vs slow-race winner -0.035 differ +0.089 = cross-race PACE, not a within-race rank). STAGE-2 windowed features (strictly prior-date, date-grouped compute-before-update, current race excluded, clip applied): base — sf_last/sf_best3/sf_avg3/sf_trend/sf_vs_classpar (class_par = mean clipped fig by class, train-only, monotone class1 +0.0113 -> class7 -0.0100); going×horse interaction — sf_on_todays_going (prior figs on today's band only), sf_going_delta (on-going minus overall avg = ground-suitability), going_switch_flag. Null on debut/insufficient history (base 90.5%, trend 82%, going-interaction 66.4%). LEAKAGE PROOF (within-race Spearman vs pos, anchors REPRODUCED or -0.131 / rpr -0.911): all sf features pre-race-like — sf_avg3/sf_vs_classpar -0.195 (strongest, place_rate-like = a touch stronger than or, EXPECTED for genuine past speed), sf_last/best3 -0.17, sf_on_todays_going -0.133, sf_trend -0.057, sf_going_delta -0.030, going_switch +0.035 — NONE near rpr, NO leak. KEY DIAGNOSTIC (ground-interaction beyond plain figure): sf_going_delta partial on sf_avg3 = -0.033 ≈ its raw -0.030 -> ORTHOGONAL to the base figure (doesn't collapse) but TINY (6x weaker than base -0.195). All features VARY within race (>=97%, switch-flag binary 77.7%) so identifiable in conditional logit. SCORING (`models/score_speedfig_lay.py`, numpy; flat-figured subset sf_is_flat==1; train<=2023/val2024/holdout25-26 separate, frozen; SOLO for clean attribution). === THE HEADLINE (DIFFERENT from rolling/handicap) === FIRST POSITIVE BLEND-WEIGHT MOVEMENT in the project: on the matched flat-figured subset baseline {or,draw,lbs,age}=10.49% -> speedfig-full=13.47% = +2.98% (base-only-no-going 13.37%). The CLOCK is a BETTER model-leg ingredient than the handicapper's ratings — the "different information source" PRIOR WAS CORRECT IN DIRECTION. CAVEAT: partly baseline-weakness, not pure speedfig strength — `or` is handicap-only so it's race-mean-imputed/degraded on non-handicap flat, while sf is 90% populated; apples-to-apples is +2.98% (do NOT quote 13.47% vs the 6.9% HANDICAP reference — different/larger subset). RE-CHECKED leakage on the rise (prior discipline): NOT a within-race-ranking leak (proof above puts worst feature at -0.195, order of magnitude from rpr). BUT still NOT edge: blend Brier does NOT beat BSP (val .08602 vs .08574; holdout .08718 vs .08700) — the better model leg is absorbed into the blend, market already prices the speed info. LAY KILL-TEST (struck=wap, settle=bsp, 5% comm; lay-ALL@BSP NULL added to the rig): NULL lay-ALL@BSP = -3.35% val / -7.02% holdout (= commission drag, correct); lay-ALL@wap baseline +9.44%/+5.21% ROI & +20%/+17% mean CLV is the documented WAP-vs-close TIMING ARTIFACT (median drift +15%/+12.5%), common to everyone, NOT skill (proven by @BSP null being negative); blend lay-selected beats wap-baseline by only +2.6/+2.3pts and blend is 87-90% market (= market filter, not speed signal); stage1-alone lay-selects WORSE than lay-ALL in val (+5.20 vs +9.44) — documented Stage-1 negative standalone info again. GOING-INTERACTION at betting level: base-only 13.37% -> full 13.47% = +0.11% — the orthogonal-but-tiny going_delta whisper VANISHES once blended; sf_on_todays_going (+0.161 beta) just steals collinear weight from base figs; ground-suitability is REAL BUT NEGLIGIBLE. VERDICT: leakage-clean, INFORMATIVE (first positive blend movement, clock > ratings as a model ingredient) but ALREADY PRICED — no market-orthogonal edge, does NOT beat BSP, lay edge dies at the @BSP null; does NOT justify paid data. CROSS-CUTTING: THREE free families now tested (rolling form, handicap structure, speed figures) — ALL real, ALL priced; speed figures are the strongest model-leg ingredient found but the market prices the clock too. Public-information edge looks comprehensively ruled out on this market. Derived `joined_gb_2018_2026_sf.csv` gitignored (regenerable); modules committed. `Free_Experiment_Catalogue.md` STILL MISSING from repo — flag if it reappears.
- 2026-06-22 — Handicap feature set (`features/build_handicap.py`, own module, sibling of build_rolling, NOT fused) built + tested on the LAY target, then REVERTED-in-spirit (machinery kept, no edge). Scope = FLAT handicaps only (type==Flat AND race_name~handicap/hcap): 339,915 rows = 71.1% of flat, OR 100% populated — matches the audit. THREE dimensions, all runner-VARYING and leakage-safe (strictly-prior flat-hcap runs, date-grouped compute-before-update, current OR/lbs/class are pre-race PUBLISHED so clean): OR (h_or, h_or_vs_lastwin "well-in", delta_last/career, vs_pb, rank, z, hcap_runs), WEIGHT (h_lbs, h_lbs_z, h_lbs_delta_last), CLASS (h_class_delta_last, h_class_drop_best). Race-CONSTANT features (h_or_spread/std/field_or_mean, h_class) kept in the FILE for Stage-2 but OUT of Stage-1 (they cancel in a conditional logit). LEAKAGE PROOF (within-race Spearman vs pos, vs benchmarks or=-0.117 clean, rpr=-0.889 leak): every feature lands in or's mild band (well-in -0.090, or_delta_last -0.160 strongest, weight ~-0.111 ≈or by collinearity, class trajectory ~-0.02/+0.00 near-zero) — NONE near rpr. Clean. SCORING (out-of-sample: train<=2023, val2024, holdout25-26 separate; handicap-features SOLO for clean attribution; `models/score_handicap_lay.py`, numpy). THE ONE NUMBER — Stage-1 model weight in the Benter blend: baseline {or,draw,lbs,age}=7.02% (≈the 6.9% reference, harness validated) -> handicap-solo=3.11% (movement -3.90pts). Handicap features make Stage-1 a WORSE blend partner — same pattern as the rolling batch (0.078->0.016). Blend Brier approaches but does NOT beat BSP (val .08718 vs .08699; holdout .08835 vs .08818) — no red flag. LAY target headline numbers looked LARGE (lay-ALL baseline +8.54% ROI / +20% mean CLV val) -> per the rpr lesson, re-checked leakage instead of celebrating: it is a STRUCK-PRICE TIMING ARTIFACT, not edge. Proof: lay-ALL settled AT BSP (strike=settle=close) = -4.63% ROI ≈ -commission (correct); the +8.54% appears ONLY striking at WAP (median 15% shorter than BSP — the documented 12-16% WAP-vs-close gap) and settling at BSP, i.e. front-running the drift. It is in the lay-ALL baseline, common to everyone. Surviving the artifact, both signs are NEGATIVE for the features: blend lay-selected beats lay-ALL by only ~+1.3-1.7pts and the blend is 97% market (so that lift is a market filter, not handicap signal); Stage-1-alone lay-selects WORSE than laying everything (val 5.14% vs 8.54%) — anti-predictive, consistent with the documented Stage-1 negative standalone information. VERDICT (prior held exactly): leakage-clean, modest, ALREADY PRICED — handicap structure adds no market-orthogonal signal; does NOT justify paid data, same as rolling. LAY KILL-TEST established as reusable infra (`score_handicap_lay.py`): the correct null is lay-ALL@BSP = -4.63% (= -commission); any large lay CLV/ROI at WAP is the WAP-vs-close timing artifact (common to the lay-ALL baseline), NOT skill — re-run this before believing any lay number. CROSS-CUTTING: two free feature families now tested (rolling form, handicap structure) — BOTH real-but-priced. Public-information edge is increasingly ruled out on this market; the remaining free shot is a DIFFERENT information source (time, not ratings) = speed figures. Derived `joined_gb_2018_2026_hcap.csv` is gitignored (data/joined/, regenerable); modules committed. NOTE: `Free_Experiment_Catalogue.md` is MISSING from the repo (user re-adding it) — flag if it reappears.
- NEXT SESSION: speed figures DONE (see 2026-06-23 above) — all three free families (rolling, handicap, speed) now tested, all real-but-priced. Public-information edge comprehensively ruled out on this market via free data. OPEN DIRECTIONS: (a) combine families in one Stage-1 (rolling+handicap+speed fused) — unlikely to beat BSP given each is priced, but confirms the ceiling; (b) the only remaining shots are NON-public/orthogonal-to-the-pre-off-market data (timestamped strike prices for a true CLV, in-running, or genuinely private signals) — i.e. money/data, not more public features; (c) git identity still set to personal account email — decide before pushing anywhere.

- 2026-06-21 — WAP (PPWAP) carried into the join -> CLV now scores on a REAL struck price; the interim struck-price problem is SOLVED. `offline_join.py` copies `wap`/`morning_wap`/`morning_vol` from the Betfair cache and derives `wap_valid` (1 iff wap>1.0); all years 2018-2026 re-joined uniformly via offline_join (2018-2023 inline BSP reproduced 0-mismatch) and recombined via `combine_years.py` -> `joined_gb_2018_2026.csv` (52 cols, wap_valid 99.4%). Pipeline default `STRUCK_MODE=wap` (geomean/pre_min/pre_max kept for comparison); stage1 carries `score_wap`, stage2 blend + CLV scorers strike on wap. RESULT: the struck-proxy swing collapsed from ~27 points (geomean +2.63% -> pre_min -24.87%) to ONE trustworthy number — Blend CLV -1.00% mean / -0.38% median, 47.9% beat close. No edge, consistent with proper scores (Blend Brier .08721 > BSP .08694). HONEST LIMIT (keep explicit): WAP is volume-averaged over the whole pre-off window (median 12-16% from BSP), so CLV-vs-BSP still carries a WAP-vs-close TIMING component — legitimate interim struck price, NOT a timestamped strike, NOT the final word on edge; `morning_wap` carried as an earlier comparison point. REPO: first commit ever landed this session (git init, a352f92) — no version history exists before this point, clean slate. OPEN (next session): git identity is set to the personal account email — decide whether to keep before pushing anywhere.
  - morning_wap analysis (earlier struck-price comparison; READ-ONLY re-pricing of the SAME committed selections, no retrain). Blend morning CLV +6.20% (vs pre-off wap -1.00%) is a pure TIMING OFFSET, NOT edge: blend Brier is invariant under struck re-pricing (we only swap the struck leg), market-implied Brier ranks morning .08881 > wap .08751 > BSP .08724 (morning least efficient), and the Blend's picks STEAM into the close (selection drift wap/morning-1 median -3.38% vs all-runner baseline -1.04%) — i.e. on the RIGHT side of the morning->close move, so striking early just front-runs the steam. Stage-1 morning CLV is WORSE (-17.19% vs pre-off -13.50%): the sign FLIPS because Stage-1 picks DRIFT out (selection drift +31.66% mean / +11.80% median vs baseline +14.12% / -1.04%) — WRONG side of the informed move. Stage-1 therefore has NEGATIVE standalone information: it backs longshots / horses the market correctly marks down (the FLB/public-bias trap operating on our own weak public-feature model — or/draw/lbs/age, often imputed outside handicaps). NET: Blend = well-behaved market-dominated estimator; Stage-1 = worse than market, on the wrong side of the informed move. Both expected for public features. Measurement infrastructure confirmed working. morning_wap kept as a price-path reference only; honest interim CLV remains pre-off wap (-1.00%).
- 2026-06-18 — Stage-2 Benter blend (Blocks D1–D3) built. `models/stage2_blend.py` blends log(model_prob)+log(market_prob from pre-off LTP, NOT BSP) via per-race conditional logit. Learned weights: market b≈0.98 vs model a≈0.026 (~97%/3%; robust to L2) → model added only a sliver. Three-way (`models/score_blend_clv.py`): blend BEATS Stage-1-alone (Brier .0863 vs .0960, log-loss 1.787 vs 2.107) and APPROACHES but does not beat BSP (Brier .08629 vs BSP .08627); CLV mean −0.10% vs Stage-1 −0.38%. No red flags. Pipeline proven end-to-end; not a real edge (concept demo). Phase 3 (edge features) NOT started.
- 2026-06-18 — Stage-1 model (Blocks A–C) built. Conditional logit (`models/stage1_logit.py`) on pre-race features `or/draw/lbs/age`; `rpr` EXCLUDED as post-race leakage (top-rpr wins 72.9%, Spearman −0.886 vs pos; diagnostic confirmed `or` is clean pre-race). Calibration (`models/calibrate.py`): model calibrated but worse than BSP on proper scores (Brier .096 vs .086, log-loss 2.11 vs 1.78) — under-confident/low-resolution. CLV (`models/score_stage1_clv.py`): does NOT beat BSP (mean CLV −0.38%, beats close 36.8%) — correct/expected for public features. Stage 2 not started.
- 2026-06-16 — CLV harness (`backtest/clv.py`) and unit tests (`backtest/test_clv.py`, 10 tests) built and passing; run via `python3 run_tests.py`. Data foundation Blocks 1–4 complete: joined GB Sept-2025 dataset in `data/joined/`.
